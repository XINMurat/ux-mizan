# Normalize text to LF in the repo; check out native on Windows.
* text=auto eol=lf

# Scripts that must run as LF regardless of platform.
*.py text eol=lf
tools/hooks/pre-commit text eol=lf

# Binary skill package — never touch line endings / diff as text.
*.skill binary
